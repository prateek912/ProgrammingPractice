package com.circularQueue.prac;

import java.util.Scanner;

public class Main {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int times = sc.nextInt();
		int[] queue = new int[times];
		
		int front = 0;
		int rear = 0;
		
		while(times > 0) {
			String str = sc.next();
			int element = sc.nextInt();
			
			if("E".equalsIgnoreCase(str)) {
				pushToQueue(element,rear,queue.length,queue);
				rear++;
			}else if("D".equalsIgnoreCase(str)) {
				popFromQueue(front,rear,queue.length,queue);
				front++;
			}
			
			times--;
		}
	}
	
	public static void pushToQueue(int element, int rear, int size,int[] queue) {
		// Check to see if Queue is full
		if(rear != size) {
			queue[rear] = element;
		}else {
			System.out.println("Queue is OverFlowed!!");
		}
	}
	
	public static void popFromQueue(int front, int rear, int size, int[] queue) {
		// Check to see if it is not already empty
		if( front != rear ) {
			
		}else {
			System.out.println("Queue is UnderFlow!!");
		}
	}

}
